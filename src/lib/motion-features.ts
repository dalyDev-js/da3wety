import { domAnimation } from "motion/react";

// Feature bundle for LazyMotion: animations, variants, exit, tap/hover/focus gestures.
// Switch to `domMax` only if drag or layout animations are ever needed.
export default domAnimation;
